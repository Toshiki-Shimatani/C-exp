NAME:
	exprn

SYNOPSIS:
	exprn <expression>

DESCRIPTION:
	Exprn command is a same command as the expr command,
	except for that it prints the evaluated value with no NEWLINE.
	See expr command for details.

EXAMPLES:
	2SECONDS> exprn ( and %B2%11 %B2%00 ) ; expr ( or %B2%11 %B2%00 )
	%B2%00%B2%11
	2SECONDS> exprn ( string abc ) ; expr ( string def )
	' abc '' def '
	2SECONDS>

NOTES:

SEE ALSO:
	EXPR(command)
	SETBASE(command)
