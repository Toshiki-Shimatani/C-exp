NAME:
	mon_rmv

SYNOPSIS:
	mon_rmv <key>

DESCRIPTION:
	Mon_rmv command unregisters a monitor cell, which is a condition
	expression on which forward command must breaks, in the
	monitor list. The <key> is a name which identifies the cell
	in the list.

EXAMPLES:
	2SECONDS> monitor do
	mon1  true
	mon2  false
	2SECONDS> mon_rmv mon1 mon2 ; monitor do
	2SECONDS>

NOTES:

SEE ALSO:
	MONITOR(command)
	MON_ADD(command)
	MON_ON(command)
	MON_OFF(command)
	MON_STAT(command)
