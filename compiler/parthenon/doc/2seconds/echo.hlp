NAME:
	echo

SYNOPSIS:
	echo [<token> ...]
	echo

DESCRIPTION:
	Echo command outputs the given token(s) to speaking-stream.

	The seconds synopsis (without any arguments) outputs
	a empty line to the stream.

EXAMPLES:
	2SECONDS> echo check test ; echo file
	check test
	file
	2SECONDS> echo

	2SECONDS>

NOTES:
	* In the stream the tokens will be delimited by ONE blank,
	  and teminated by a NEWLINE.

SEE ALSO:
	ECHON(command)
	ECHOC(command)
	ECHOCN(command)
