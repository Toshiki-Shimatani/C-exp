 out_blif [<path_name>]

        Write the network in BLIF format consist of simple gates and complex 
        gates.
        On-set is used to express each gate function.

        If <path_name> is not specified, output is written to standard output.

 out_blifs [<path_name>]

        Write the network in BLIF format consist of simple gates.
        On-set or off-set is used to express each gate function.

        If <path_name> is not specified, output is written to standard output.
