  lib コマンド

  (A) 機能

  回路の改善に使用するセルを読み込むためのコマンドです。

  (B) 形式

  lib lib { < ディレクトリ名 > }

  (C) パラメータ

  ディレクトリ名： セルデータが格納されたディレクトリ名を指定します。 複数個
  指定可能です。

  (D) 注意事項

  ●同一のセルが存在した場合は、最初に読み込んだものが有効です。この時、ワー
    ニングが出力されます。

  ●セルデータを格納したディレクトリには、セルの pcd  ファイル以外のものを入
    れないでください。

  ●最初の形式では、opt_map のライブラリデータベースに登録されているセルを表
    示します。

  ● opt_map のライブラリデータベースへのセルの登録は opt_map の起動時にも起
    こります。 opt_map が読み込んだ全ての pcd ファイルに対してライブラリデー
    タベースへのセルの登録が行われます。

  (E) 関連事項

  sort

