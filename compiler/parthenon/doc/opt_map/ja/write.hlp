  write コマンド

  (A) 機能

  変更を受けたネットリストを出力するコマンドです。

  (B) 形式

  write < ディレクトリ名 >

  (C) パラメータ

  ディレクトリ名： ネットリストを書き込むディレクトリ名を指定します。

  (D) 注意事項

  ●フリーズされていない全ての nld  モジュールのネットリストが出力されます。
    opt_map  起動時には同一のクラスであった nld モジュールも変更を受けたもの
    として、新しい別々のクラス名が付与されます。

  ●どの pcd モジュールが使われるかは  opt  コマンドにより変更を受けますが、
    pcd モジュールのクラス名はモジュール名のままで、変更されません。

  (E) 関連事項

  writem 、last 、lastm

