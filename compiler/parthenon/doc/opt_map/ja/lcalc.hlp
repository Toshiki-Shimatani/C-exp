lcalc コマンド

(A) 機能
　　回路全体の各ネットに接続された端子の負荷を合計し、各ネットの負荷を計算し
　  ます。

(B) 形式

　　lcalc

(C) パラメータ
　　なし

(D) 注意事項
　　set コマンド等で、端子の負荷を更新した場合等は、遅延計算、制約条件のチェ
　  ックを行う前に、本コマンドによってネットの負荷を計算しておく必要がありま
    す。

　　opt コマンドの実行中にも、必要に応じて本コマンドと同様の負荷計算が行われ
　  ます。  opt コマンドの実行により何らかの回路変更が起こった場合には、それ
    を反映した最新値が計算された状態になっていますので、opt コマンド実行直後
    に本コマンドを実行する必要はありません。


(E) 関連事項
　　init コマンド
　　set コマンド
　　opt コマンド

