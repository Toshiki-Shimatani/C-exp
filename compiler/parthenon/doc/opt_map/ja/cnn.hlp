  cnn ( change name normal ) コマンド

  (A) 機能

  全ての名称を a 、b 、c 、...z 、A 、B 、C 、...Z 、_  のみで構成されたもの
  に変更します。

  (B) 形式

  cnn

  (C) パラメータ

  なし。

  (D) 注意事項

  ●変更される名称は、クラス名 (モジュール名) 、サブモジュール名、ピン名です。
    必要であれば、pcd モジュールのクラス名、フリーズされたモジュールのクラス
    名、pcd モジュールのピン名、フリーズされたモジュールのピン名、フリーズさ
    れたモジュールのサブモジュール名も変更されます。その様な場合にはメッセー
    ジが出力されます。

  ● [、] 、-、などは、_ 、にされます。 単純に変更された名称がすでに存在する
     名称と同じになる場合は

	単純変更名称 _ 数

     とされます。

  (E) 関連事項

