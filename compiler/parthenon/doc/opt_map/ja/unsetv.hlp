  unsetv コマンド

  (A) 機能

  グローバルに存在する変数の値を取り除くコマンドです。

  (B) 形式

  unsetv ? < 変数名 >

  (C) パラメータ

  変数名： opt_map 起動時に読み込んだ回路、あるいは、lib  コマンドで読み込ん
  だライブラリに含まれる変数の名前です。

  (D) 注意事項

  (E) 関連事項

  lv 、setv

