  lp コマンド

  (A) 機能

  カレントモジュールのピンを表示するコマンドです。

  (B) 形式

  lp

  (C) パラメータ

  なし。

  (D) 注意事項

  ● opt_map  の内部データには他のデータから計算によって導き出すことができる
    ものがあり、その値はいつも最新値であるとは限りません。 その様なものには、
    lcalc コマンドによって更新されるネットの負荷容量、dcalc コマンドによって
    更新される遅延時刻、 scalc コマンドによって更新される power 、gates 、
    area および nld モジュールのインタフェースピンタイプがあります。

  ●ピンタイプの最新値は、scalc コマンド実行後の、lp コマンドより表示されま
    す。

  (E) 関連事項

  scalc 、scalcm
