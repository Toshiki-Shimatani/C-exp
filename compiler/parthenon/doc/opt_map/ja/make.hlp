  make コマンド

  (A) 機能

  カレントモジュール内にサブモジュールを追加するコマンドです。

  (B) 形式

  make < クラス名 > < サブモジュール名 >

  (C) パラメータ

  クラス名： 追加されるサブモジュールのクラス名

  サブモジュール名 追加されるサブモジュールのサブモジュール名

  (D) 注意事項

  ●追加されるサブモジュールのクラス名は全体で使われていないものでなければな
    りません。

  ●追加されるサブモジュールのサブモジュール名はカレントモジュール内で使われ
    ていないものでなければなりません。

  ●カレントのモジュールは freeze されていてはなりません。

  (E) 関連事項

  encc 、encs 、disc 、diss 、flat

