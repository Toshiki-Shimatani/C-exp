  lv コマンド

  (A) 機能

  グローバルに存在する変数とその値を表示するコマンドです。

  (B) 形式

  lv

  (C) パラメータ

  なし。

  (D) 注意事項

  ●変数とは、opt_map プログラムの起動時に読み込んだ pcd モジュール、 あるい
    は、 lib コマンドで読み込んだ pcd モジュールに含まれる変数のことです。変
    数はその名称が同一であれば、全て同じものとして扱われます。

  ●値がまだセットされていない時は 'UNSET' と表示されます。

  (E) 関連事項

  setv 、unsetv

