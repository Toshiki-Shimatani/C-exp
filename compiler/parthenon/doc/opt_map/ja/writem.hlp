  writem コマンド

  (A) 機能

  カレントモジュールのネットリストを標準出力に出力するコマンドです。

  (B) 形式

  writem

  (C) パラメータ

  なし。

  (D) 注意事項

  ●カレントモジュールに含まれる複数のフリーズされていない nld モジュールが、
    opt_map 起動時には同一のクラスであった場合でも、変更を受けたものとして、
    新しい別々のクラス名が付与されます。

  ●どの pcd モジュールが使われるかは  opt  コマンドにより変更を受けますが、
    pcd モジュールのクラス名はモジュール名のままで、変更されません。

  (E) 関連事項

  write 、last 、lastm

