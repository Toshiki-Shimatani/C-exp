  flat コマンド

  (A) 機能

  カレントモジュール内の階層を全て取り払うコマンドです。

  (B) 形式

  flat

  (C) パラメータ

  なし。

  (D) 注意事項

  ● diss コマンドを階層がなくなるまで実行するのと等価です。

  ● freeze されたサブモジュールの階層はなくなりません。

  ●カレントのモジュールは freeze されていてはなりません。

  (E) 関連事項

  make 、encc 、encs 、disc 、diss

