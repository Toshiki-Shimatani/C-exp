lfrz command

(A) FUNCTION
    Lfrz command lists all frozen submodules in current module.

(B) SYNOPSIS

    lfrzs

(C) PARAMETERS
    None.

(D) NOTES
    The command is invalidated in frozen submodule.

    The command traverses not only submodules in current module but
    any submodule under current module.

    The command does not list submodules in a frozen submodule.

(E) SEE ALSO
    frzs command
    frzc command
    mlts command
    mltc command
