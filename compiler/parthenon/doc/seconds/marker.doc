marker command

    (A) FUNCTION
        Marker command shows or changes the marker position.

    (B) SYNOPSIS

         marker  [ <position> ]

    (C) PARAMETERS
        <position>
         tells the position to which the marker is moved,
         which is specified as an arbitrary facility.    

         Current position of the marker is shown if the <position>
         is omitted.

    (D) NOTES
        On the first use of install command the marker is initialized
        with the root ('/') of simulation image

    (E) SEE ALSO
        move command
