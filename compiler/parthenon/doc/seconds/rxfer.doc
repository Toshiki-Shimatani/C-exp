rxfer command

    (A) FUNCTION
        Rxfer command shows the read cross reference of specified
        facility, in which each of the facilities that dpend on value
        of the specified facility is listed with its <condition> of
        the need and <action> on it.

    (B) SYNOPSIS

         rxfer <facility>

    (C) PARAMETERS
        <facility>
         specifies name of the facility on which the cross reference
         is shown. It must be with a facility with value, such as
         a register or a terminal.

    (D) NOTES

    (E) SEE ALSO
        wxfer command
