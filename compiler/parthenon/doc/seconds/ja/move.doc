move コマンド

　　(A)  機能
   　　　現在位置を出力/変更します。

    (B)  形式

         move　[ <position> ]

　　(C)  パラメータ
　　　  <position>
          現在位置の移動先を指定します。
          移動先は、任意のファシリティです。
          <position> が省略されると、現在の "現在位置" を出力します。

　　(D)  注意事項
　　　   最初の install コマンドにより、現在位置は、シミュレーションイメージ
         の根 ( '/' ) に設定されます。

    (E)  関連事項
         2.1 SFL の内部表現
         marker コマンド

