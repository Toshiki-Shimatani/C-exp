claim コマンド

　　(A) 機能
　　　　以後のコマンドのエラーメッセージの出力先 ( claiming-stream )  を切り
　　　  換えます。

    (B) 形式

　　　　claim　[ <file> ]

　　(C) パラメータ
　　　<file>
        コマンドのエラーメッセージの出力先ファイルを指定するパス名です。パス
        名は、ファイルシステムでの指定方法に従います。<file>  を省略すると、
        以後のエラーメッセージは、SECONDS プロセスの stderr に出力されます。

　　(D) 注意事項
　　　<file> で指定されたファイルが既に存在する場合、そのファイルはオーバ−
　　　ライトされます。
　　　<file> で指定されたファイルが存在しない場合、 ファイルが新たに作成され
　　　ます。

    (E) 関連事項
      claima コマンド
　　　speak コマンド



