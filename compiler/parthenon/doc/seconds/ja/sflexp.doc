sflexp コマンド

　　(A)  機能

 　　　  SFL ソースを読み込み、ネットリスト表現 ( HSL 形式) を作成します。

　　(B)  形式

　　　　 sflexp <sfl_file_name> <hsl_file_name>
         sflexp . <hsl_file_name>

　　(C)  パラメータ

　　　   <sfl_file_name>
           SFL ソースファイル名を指定します。
         <hsl_file_name>
           ネットリストを出力すべき HSL ファイル名を指定します。

           2 番目の形式は、直前の sflread コマンドまたは sflexp コマンドで指
          定した  SFL ソースファイル名と同一のファイルを指定する場合に使用し
          ます。

　　(D)  注意事項

         SFL ソースファイルに誤りがある場合は、

       Error detected ! Shall I open the edit pad ? Type <CR> if ready.

         のメッセージが出力されますので、< CR > を入力してください。そうする
         とエディタが起動され、カーソルがエラ−位置の直後に移動し、　エラ−
　　　　 箇所を知らせます。エラーを修正したら、エディタを ^Y または < SAVE >
         ( Apollo  のディスプレーマネージャのエディタ終了コマンド) によって
         終了させてから、r. と入力してください。r. は sflexp コマンドのサブ
         コマンドで、 retry を表わします。この時点で入力可能なサブコマンドは

            r.： retry (再試行)
	    a.： abort (中断)
	    h.： help

         の 3 つだけです。 a.を入力すると、 sflexp コマンドはその時点で終了
         します。

         SFL ソースファイルに間違いが無い時は、このメッセージは出力されませ
         ん。なお sflexp コマンドは非常に時間を要します。

         また sflexp コマンドのサブコマンドを入力中に

         prolog: syntax error at LINE=2, COLUMN=0

         の様なメッセージが出力された時は、.< CR > を何度か入力し、: が入力 
         プロンプトとして出力されてから、サブコマンドを入力してください。

　　(E)  関連事項

         sfldemo コマンドは sflexp コマンドと同様に機能しますが、途中経過を
	 詳しく表示します。


    (G)  制限事項

    (1)
        出力端子の参照、制御出力端子の参照、サブモジュールの制御入力端子の参
        照、機能回路の制御入力端子の参照を行うと、等電位面に対し複数のネット
        名が付与されてしまう事があります。これは、PARTHENON 内のツールに対し
        ては、問題となりません。

    (2)
        出力が全く使用されないゲート等が出力されてしまうことがあります。これ
        は、実部品割り付けプログラムにより削除されますので、問題ありません。
