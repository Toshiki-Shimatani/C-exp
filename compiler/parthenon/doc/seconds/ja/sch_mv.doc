sch_mv コマンド

　　(A)  機能
　　　 　スケジュールリストのセルを移動します。

    (B)  形式

　　　　 sch_mv <time> <key> <new_time> [ <after> ]

　  (C)  パラメータ
　　　   <time>
           移動する前のスケジュールリストを時刻により指定します。
           時刻は、符号なし整数または + 符号つきの整数で、 + 符号つきの整数
           は、シミュレーション時刻にその整数を加えた時刻のスケジュールリス
           トとなります。

　　　   <key>
           移動するスケジュールセルの識別名を指定します。

　　　   <new_time>
           移動先のスケジュールリストを時刻により指定します。
           時刻は、符号なし整数または + 符号つきの整数で、 + 符号つきの整数
           は、シミュレーション時刻にその整数を加えた時刻のスケジュールリス
           トとなります。

　　　   <after>
           移動先のスケジュールリストでのセルの位置を、すでにそのリストにあ
           るセルの識別名で指定します。セルは、この識別名のセルの後ろに移動
           します。
           <after> が指定されなかった場合は、リストの先頭に置かれます。また
           指定された識別名を持ったセルが存在しない場合には、リストの末尾に
           置かれます。

　　(D)  注意事項
         <new-time> は、シミュレーション時刻からみて、未来の時刻を示していな
         ければなりません。


　　(E)  関連事項
         2.4.1 スケジュール機能
	 sch_rmv コマンド
	 sch_add コマンド
	 schedule コマンド




