move command

    (A) FUNCTION
        Move command shows or changes current postion.

    (B) SYNOPSIS

        move  [ <position> ]

    (C) PARAMETERS
        <position>
         tells the position to which the current is moved,
         which is specified as an arbitrary facility.    
         
         Current position of the marker is shown if the <position>
         is omitted.

    (D) NOTES
        On the first use of install command the current is initialized
        with the root ('/') of simulation image

    (E) SEE ALSO
        marker command
