;***************************************************
;* (C)Copyright by NTT DATA Corp.1994(unpublished) *
;*                        All rights are reserved. *
;***************************************************

(def-module an1_i5 power 12 area 1.1 gates 4
  
    (def-pin a1 type input load 0.12)
    (def-pin a2 type input load 0.12)
    (def-pin a3 type input load 0.12)
    (def-pin a4 type input load 0.12)
    (def-pin a5 type input load 0.12)
    (def-pin z type output load 0.098)

    (def-symbol AND a1 a2 a3 a4 a5 z)

    (def-function z (and a1 a2 a3 a4 a5))

    (def-delay /a1 /z (+ 1.1 (* 3.0 ~z)))
    (def-delay /a2 /z (+ 1.1 (* 3.0 ~z)))
    (def-delay /a3 /z (+ 1.1 (* 3.0 ~z)))
    (def-delay /a4 /z (+ 1.1 (* 3.0 ~z)))
    (def-delay /a5 /z (+ 1.1 (* 3.0 ~z)))
    (def-delay %a1 %z (+ 1.1 (* 3.0 ~z)))
    (def-delay %a2 %z (+ 1.1 (* 3.0 ~z)))
    (def-delay %a3 %z (+ 1.1 (* 3.0 ~z)))
    (def-delay %a4 %z (+ 1.1 (* 3.0 ~z)))
    (def-delay %a5 %z (+ 1.1 (* 3.0 ~z)))

	(def-constraint drive (- 0.67 ~z) type max)
	(def-constraint drive (- ~z 0) type min)
)
