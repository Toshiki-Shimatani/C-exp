;***************************************************
;* (C)Copyright by NTT DATA Corp.1994(unpublished) *
;*                        All rights are reserved. *
;***************************************************

(def-module an1_i2 power 5.5 area 0.49 gates 2
  
    (def-pin a1 type input load 0.12)
    (def-pin a2 type input load 0.12)
    (def-pin z type output load 0.086)
   
    (def-symbol AND a1 a2 z)

    (def-function z (and a1 a2))

    (def-delay /a1 /z (+ 0.52 (* 1.8 ~z)))
    (def-delay /a2 /z (+ 0.52 (* 1.8 ~z)))
    (def-delay %a1 %z (+ 0.52 (* 1.8 ~z)))
    (def-delay %a2 %z (+ 0.52 (* 1.8 ~z)))

	(def-constraint drive (- 1.1 ~z) type max)
	(def-constraint drive (- ~z 0) type min)
)
