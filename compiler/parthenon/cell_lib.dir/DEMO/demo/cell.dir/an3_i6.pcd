;***************************************************
;* (C)Copyright by NTT DATA Corp.1994(unpublished) *
;*                        All rights are reserved. *
;***************************************************

(def-module an3_i6 power 26 area 2.5 gates 10
  
    (def-pin a1 type input load 0.23)
    (def-pin a2 type input load 0.23)
    (def-pin a3 type input load 0.23)
    (def-pin a4 type input load 0.23)
    (def-pin a5 type input load 0.23)
    (def-pin a6 type input load 0.23)
    (def-pin z type output load 0.20)

    (def-symbol AND a1 a2 a3 a4 a5 a6 z)

    (def-function z (and a1 a2 a3 a4 a5 a6))

    (def-delay /a1 /z (+ 1.1 (* 1.0 ~z)))
    (def-delay /a2 /z (+ 1.1 (* 1.0 ~z)))
    (def-delay /a3 /z (+ 1.1 (* 1.0 ~z)))
    (def-delay /a4 /z (+ 1.1 (* 1.0 ~z)))
    (def-delay /a5 /z (+ 1.1 (* 1.0 ~z)))
    (def-delay /a6 /z (+ 1.1 (* 1.0 ~z)))
    (def-delay %a1 %z (+ 1.1 (* 1.0 ~z)))
    (def-delay %a2 %z (+ 1.1 (* 1.0 ~z)))
    (def-delay %a3 %z (+ 1.1 (* 1.0 ~z)))
    (def-delay %a4 %z (+ 1.1 (* 1.0 ~z)))
    (def-delay %a5 %z (+ 1.1 (* 1.0 ~z)))
    (def-delay %a6 %z (+ 1.1 (* 1.0 ~z)))

	(def-constraint drive (- 2.0 ~z) type max)
	(def-constraint drive (- ~z 0) type min)
)
