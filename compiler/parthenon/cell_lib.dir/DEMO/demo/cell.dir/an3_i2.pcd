;***************************************************
;* (C)Copyright by NTT DATA Corp.1994(unpublished) *
;*                        All rights are reserved. *
;***************************************************

(def-module an3_i2 power 11.0 area 1.0 gates 4
  
    (def-pin a1 type input load 0.24)
    (def-pin a2 type input load 0.24)
    (def-pin z type output load 0.017)
   
    (def-symbol AND a1 a2 z)

    (def-function z (and a1 a2))

    (def-delay /a1 /z (+ 0.52 (* 0.59 ~z)))
    (def-delay /a2 /z (+ 0.52 (* 0.59 ~z)))
    (def-delay %a1 %z (+ 0.52 (* 0.59 ~z)))
    (def-delay %a2 %z (+ 0.52 (* 0.59 ~z)))

	(def-constraint drive (- 3.4 ~z) type max)
	(def-constraint drive (- ~z 0) type min)
)
